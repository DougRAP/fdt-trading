# Acceptance checks (brief §Acceptance checks) — fixture mode

Status values: **pass** (automated evidence covers the check), **pass-with-note** (evidence is partial or manual), **not verifiable in fixture mode**.
Test evidence is `file › it() title` from `src/**/*.test.ts` (`npm test`). UI paths are manual steps in `npm run dev`.
The "Browser check (master)" column records the master's verification in headless Chromium (DevTools-protocol driven) on 2026-09-12 against the fixture build.

| # | Check | Status | Evidence | Browser check (master) |
|---|---|---|---|---|
| 1 | All six cards and both right panels fit default desktop targets; mobile has no horizontal clipping. Keyboard selection and labeled controls work. | pass-with-note | No automated layout test. Structure/labels: `smoke.test.ts › renders header, observer, ticket, results and the explanation section without throwing` (asserts `aria-pressed`, panel titles, section anchor). Keyboard: card buttons are native `<button aria-pressed>`; ArrowUp/ArrowDown handler in `ui/Observer.tsx` `onKeyDown` — not covered by a test. Fit at 1440×900 / 1280×800 / 390 wide verified only by the master's Chromium screenshots (T5.1–T5.4). | Chromium 1440×900 and 1280×800: header one row, 6 cards, ticket incl. primary action, results incl. all 6 rows visible without page scroll. 390 wide: stacks, no horizontal clipping. ArrowDown moved focus 01 NQ → 02 ES; Enter/click selected it. |
| 2 | NQ fixture: Q=1.8, A=.6 ⇒ u=3.0; prev u=2.4, σΔu=.25 ⇒ 2.4; H=.60, prev .38, σΔH=.10 ⇒ 2.2; S=2.2; long qualifies; ATR=25 ⇒ D=52.5. | pass | From full histories: `formula/compute.test.ts › NQ long: u 3.0, v 2.4, p 2.2, S 2.2, long qualifies`. From the fixture snapshot: `fixtures/snapshots.test.ts › NQ matches acceptance check #2: u 3.0, v 2.4, p 2.2, S 2.2, long qualifies, ATR 25 pts = 100 ticks`. D: `stops/stops.test.ts › ATR 25 pts (100 ticks), H .6, long => 52.5 pts = 210 ticks`. Short mirror: `formula/compute.test.ts › short mirror: H -0.60 / prev -0.38, down-bar => S 2.2, short qualifies`. | Card 01 NQ shows S 2.20, Q 1.80, A 0.60, u 3.00, H 0.60, Δu/σ 2.40, dΔH/σ 2.20, QUALIFIED; ticket shows stop 21947.75, D 52.50 pts, risk $1,060.00. |
| 3 | Highest rank below threshold produces no automatic paper trade. Missing ZN/GC breadth cannot be substituted with an attractive number. | pass | `paper/engine.test.ts › highest rank below threshold => no automatic paper trade (acceptance #3)`; `ranking/rank.test.ts › topQualified returns NQ; none => null (no forced trading)`; `fixtures/snapshots.test.ts › ZN and GC carry Q only; A/H/u/S blank; BREADTH_MODEL_UNDEFINED; excluded from selection`; `formula/compute.test.ts › BREADTH_MODEL_UNDEFINED keeps Q, blanks A/H/u/S`; `breadth/BreadthAdapter.test.ts › returns BREADTH_MODEL_UNDEFINED with blank A/H`. UI: ZN/GC cards show — with "UNAVAILABLE · breadth undefined". | Paper engine on fixture queued NQ only (top qualified). GC/ZN cards: rank —, S —, "UNAVAILABLE · breadth undefined", Q still shown. |
| 4 | Selecting a market does not record a position. Opening manual mode never sends an order. Actual fill recording and broker stop confirmation are distinct. | pass-with-note | Selection is UI-only state (`app/store.ts` `select` sets `selectedRoot`; no ledger append) — asserted structurally, not by a click test. No order path exists anywhere in the code base (no network calls; grep for `fetch`/broker returns nothing). Distinct events: `campaign/manual.test.ts › records an entry fill without any order; campaign OPEN with frozen config/snapshot and original risk` (ENTRY_FILL vs BROKER_STOP_RECORDED are separate events; `brokerStop` is null until recorded) and `› proposed vs broker stop discrepancy raises STOP UPDATE NEEDED; breach raises VERIFY BROKER` (recalculating the proposed stop leaves the broker stop unchanged). UI path: click any card → ticket changes, Journal history stays empty; "Record broker stop" is a separate control from "Record entry fill". | Selecting ES via keyboard/click: fdt.v1.manual had 0 events afterwards. No network calls observed in console. Broker stop status/price is a separate field from the fill; open state shows "Recorded broker stop — · not recorded" after a fill. |
| 5 | Long and short stops ratchet in the correct direction; rounding does not increase a previously ratcheted distance. Same-bar hindsight and gap fills have explicit tests. | pass | `stops/stops.test.ts › long: highest close 22100, ATR 30, H .7 => D 66 => 22034; then H .2 => D 51 => 22049; then ATR up => stays 22049`; `› short: lowest close 21900, ATR 30, H -.7 => D 66 => 21966; never moves up`; `› rounding never increases a previously ratcheted distance`; `› rounds long down and short up to the tick grid`; `› a stop computed from bar t's close does not apply to bar t, only to later bars`. Engine: `paper/engine.test.ts › a stop computed from bar t's close is not tested against bar t's own low (no same-bar hindsight)`; `› opening gap past the stop exits at the open - 1 tick, never at the stop price`; `› touched resting stop fills at stop - 1 tick; realized uses fills + fees`; `› close required when the new stop is beyond the close; exits at the next bar open`. | Paper demo: stop trailed upward across rising bars, then exited on touch above entry (+$2,070). Not a substitute for the unit tests, which carry the exact numbers. |
| 6 | Position sizing respects tick-rounded stop distance, multiplier, costs, risk budget, and integer contracts; zero size skips. Threshold changes are versioned. | pass | `sizing/sizing.test.ts › entry 22000.25, stop 21947.75, exit 21947.50`; `› per-contract risk 1,060,000 mils (211 ticks x 5000 + 2 x 2500)`; `› budget 2,500,000 mils => 2 contracts, planned loss 2,120,000 = 0.212% of equity`; `› qty 0 => skip with reason`; `› budget uses integer arithmetic (no float drift)`; `› ZN tick value stays exact in risk: 211 ticks x 15625 + 5000`; `numerics/money.test.ts › floorDivMils gives integer contract counts`; `paper/engine.test.ts › unsizeable top candidate falls through to the next sizeable one, with the skip recorded` (NQ skipped at a 1,000.00 budget, ES queued with 1 contract); `› all qualifying candidates unsizeable => skip with every reason listed`. Versioning: `app/store.test.ts › saving twice yields 0.1+user2 and the label is the config version; the base model version stays 0.1`; `› a campaign entered under saved settings freezes the full label` (frozenConfig.version === "0.1+user2"); `config/modelConfig.test.ts › frozen copy is independent of the live config`; `paper/engine.test.ts › a running campaign is governed by its frozen config, not the cfg passed to later bars` (stopBase changed to 0.5 after entry; stop stays 22037.50, not 22062.50). | Paper plan showed 2 contracts, $2,120.00 (2×), 0.212% of $1,000,000. Manual with no equity entered shows 1 contract + "equity not entered". Model settings → Save as new version → header shows v0.1+user1, fdt.v1.config written. |
| 7 | Manual and paper ledgers remain separate across mode changes and reloads. Repeated bar events cannot duplicate fills. Recorded partial exits reconcile remaining size and realized P&L. | pass | `ledger/ledger.test.ts › manual and paper ledgers are separate and survive save/load under different keys`; `› reloading and re-appending the same events is idempotent`; `paper/engine.test.ts › repeated bar events cannot duplicate fills or exits`; `fixtures/paperBars.test.ts › drives the engine to a stopped exit on the last bar, and repeating a bar is a no-op`; `campaign/manual.test.ts › partial exits reconcile remaining size and realized P&L (FIFO, exact mils); full exit closes`; `› duplicate event ids are no-ops`; `› one open position per mode; a second entry is refused while one is active`. Mode switching in the UI only changes which ledger is displayed (`app/store.ts` `setMode`); browser reload path verified by master (T5.4). | Manual fill → OPEN; in-session reload restored OPEN from fdt.v1.manual (4.6 KB). Paper ledger separate (fdt.v1.paper), results label switches Manual journal / Paper only. Re-running the demo on the used decision bar is skipped. |
| 8 | Stale prices label P&L stale. Missing signal inputs block new decisions without deleting existing protective stops. | pass | `campaign/manual.test.ts › isStale uses observedAt against now`; `› position summary labels P&L stale without a fresh mark and computes R from marks`; `› missing inputs freeze the proposed stop and flag DATA STALE without deleting it`; `paper/engine.test.ts › missing signal inputs freeze the resting stop (DATA STALE) but keep monitoring it` (no new decision while blocked; frozen stop still exits on touch); `paper/engine.test.ts › DATA STALE clears when valid inputs return, even if the stop does not move` (frozen stop re-issued as a trail stop; stopFrozenReason null, no flag); `stops/stops.test.ts › missing ATR or H freezes the previous stop with DATA_STALE and keeps it effective`; `formula/compute.test.ts › STALE` (stale inputs block eligibility, components still shown). Ratchet reference: `campaign/manual.test.ts › only a completed close advances the ratchet reference; a plain mark does not` (mark 22300 leaves the proposed stop at 21947.75; a recorded completed close 22300 advances it to 22247.50). Drawdown without an equity base: `ledger/ledger.test.ts › manual drawdown is unavailable until account equity is entered; no-mark points are not scanned`. UI: open manual position without a mark shows "— · no mark feed"; with an old mark shows "STALE"; "Record mark" and "Record completed close" are separate actions. | Manual open position with no mark: Mark "— · no mark feed", Unrealized "— · no mark feed", flags DATA STALE and STOP UPDATE NEEDED shown; proposed stop 21947.75 retained. |
| 9 | Realized results use actual fills and fees; drawdown uses marked account equity. No-data statistics show em dashes. | pass | `paper/engine.test.ts › touched resting stop fills at stop - 1 tick; realized uses fills + fees`; `› initial stop hit in the fill bar exits at the planned loss (2,120,000 mils)`; `› drawdown uses marked equity including the open position, not closed P&L alone`; `ledger/ledger.test.ts › external cash flows shift the peak basis instead of counting as P&L`; `› empty ledger: em dashes for statistics with no qualifying data`; `› per-root stats separate instruments; the account drawdown is not assigned per root`; `campaign/manual.test.ts › corrections supersede the original event but keep it in the log`. | Paper results after demo: Net realized +$2,070.00, Closed 1, Max account drawdown (marked) 0.21%, Marked equity $1,002,070.00; empty manual ledger shows em dashes. |
| 10 | AI text cannot modify calculator outputs or promote an invalid signal. The UI says "score", not "probability" or "confidence". | pass-with-note | No AI integration exists; `explainSnapshot` (`ui/Observer.tsx`) is a pure function of the snapshot that returns text only and has no write path to snapshots, ledgers, or config. Wording: `smoke.test.ts › renders header, observer, ticket, results and the explanation section without throwing` asserts the rendered HTML contains no "confidence" and no "probabilit…" outside the allowed negative phrases ("not a probability", "not calibrated probabilities", "No probability of profit"). This is a string assertion over the fixture render, not a test of an LLM. | No AI call exists. Rendered UI text uses "score"; footer reads "Score, not probability". Console error log empty across all drives. |

## Not covered by automated tests (honest gaps)

- Pixel fit at the two desktop targets and mobile clipping (#1): master's Chromium screenshots only.
- Keyboard arrow navigation between cards (#1): implemented, untested.
- Settings label bump `0.1+userN` and its localStorage round trip (#6): implemented in `app/store.ts`, untested.
- Browser reload persistence (#7): storage round trip is tested with an in-memory `StorageLike`; the real `localStorage` path was verified by the master in the browser.
- All checks run against synthetic fixtures; none of them says anything about market behavior.
